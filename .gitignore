# Secrets — never commit
**/.env
backend/.local-db-backup*.json
.env.deploy
.env.local
.env.*.local

# Build / deps
node_modules
.next
.medusa
dist
out

# Ops
*.log
.DS_Store

# Mimosa scanner workspace artifacts
.mimosa/

# Local dev media uploads (production stores media on S3/Backblaze)
backend/uploads
